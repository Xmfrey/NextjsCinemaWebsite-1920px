import React from "react";
import styles from "./ChipQuality.module.css";

const ChipQuality = ({ children }) => {
  return <div className={styles.ChipQuality}>{children}</div>;
};

export default ChipQuality;
